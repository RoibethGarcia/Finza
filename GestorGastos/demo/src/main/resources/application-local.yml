spring:
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/gestor_gastos}
    username: ${SPRING_DATASOURCE_USERNAME:gestor_gastos}
    password: ${SPRING_DATASOURCE_PASSWORD:gestor_gastos}

logging:
  level:
    root: INFO
    com.gestorgastos: DEBUG

app:
  cors:
    allowed-origins:
      - http://localhost:5173
      - http://127.0.0.1:5173
